export function initialMessage({ subject, topic }: { subject: string, topic: string }) {
    return `Hello, I’m Staxly.  Would you like to know more about ${topic}? I can also answer any other questions about ${subject}`
}
